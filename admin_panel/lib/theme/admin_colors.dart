import 'package:flutter/material.dart';

class AdminColors {
  // Core brand colors (matching customer app)
  static const Color primary = Color(0xFFD4A843);       // Luxor Golden Amber
  static const Color primaryLight = Color(0xFFE8C96A);
  static const Color secondary = Color(0xFF4A9FD4);     // Sky Blue
  static const Color accent = Color(0xFFE87040);        // Sunset Orange

  // Status colors
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFFCC00);
  static const Color error = Color(0xFFFF3B30);
  static const Color info = Color(0xFF5AC8FA);

  // Dark theme surfaces
  static const Color bgDark = Color(0xFF0D1117);
  static const Color sidebarBg = Color(0xFF161B22);
  static const Color cardDark = Color(0xFF1C2128);
  static const Color surfaceDark = Color(0xFF21262D);
  static const Color border = Color(0xFF30363D);
  static const Color borderLight = Color(0xFF484F58);

  // Text
  static const Color textPrimary = Color(0xFFF0F6FC);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color textMuted = Color(0xFF6E7681);
}
